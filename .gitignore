
# Local Claude Code state — never commit
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Private cost/metrics artifacts — internal financial data.
# This repo is PUBLIC; these contain real per-repo spend + PR costs. Never commit.
docs/review-pipeline-snapshot.md
docs/prds/review-cost-reduction.md
docs/metrics/
.metrics.env

# Local-eval corpus manifest — names private repos. Never commit (.eval.env.example is the template).
.eval.env

# macOS junk
.DS_Store

# Broker + infra build artefacts
broker/node_modules/
broker/dist/
infra/node_modules/
infra/Pulumi.*.yaml
.pulumi/
infra/.pulumi/
